# GenAI Evidence Data

This directory stores controlled GenAI input and output artifacts.

- `inputs/` contains deterministic evidence packages built from existing public repository artifacts.
- `outputs/` contains structured provider responses and run manifests.
- `fixtures/` is reserved for stable offline evaluation fixtures.

No API key, confidential customer information or personally identifiable information may be stored in this directory.